function y = forwardSub( L, b)
[nrow,ncol] = size(L);
y=zeros(nrow);
y(1) = b(1);
for i = 2:nrow
    T = zeros(1, i);
    T(i) = b(i);
    for j = 1 : i-1
        T(j) = -(L(i,j)*b(j));
    end
    y(sum(T)
end